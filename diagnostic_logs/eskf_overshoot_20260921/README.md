# ESKF 停止后过冲诊断（2026-09-21）

- `before_smoothing.jsonl`：加入诊断记录但尚未加入历史平滑的真实 GUI 数据，约 17 秒。用户按提示先静止、平滑移动、停止；动作时刻仅有提示时间，无外部运动真值。
- `before_smoothing.log`：本轮 GUI 日志。
- `after_smoothing.jsonl`：历史平滑后真实 GUI 两轮短采集，包括清空/重新开始和关闭 ZUPT 的检查。
- `after_smoothing.log`：完整流程验收通过记录。
- `after_smoothing.png`：新版 GUI 六图截图。

修复前相对首帧 4.64 秒处零速更新的位置修正模长约 3.98 mm，同步的相邻显示点距离约 3.75 mm。其后静止段未复现截图中厘米级持续漂移。说明此次实测存在“历史点未同步修正”的回折，不能认定这解释了所有用户报告的过冲。

同一段数据离线回放到该次更新，用交叉协方差同步修正历史位置后，窗口内最大相邻点距离约 0.499 mm。最终端点与原滤波器输出差为零。数字描述的是估计曲线连续性，不是实测定位误差。

新版实机 705 帧、50 Hz、I²C/TX=0/0，零速/静止观测 507/100，轨迹保留 501 点/10 秒；开始、停止、清空、重启、关闭 ZUPT、配置刷新、文件关闭及 SSH/工作线程/远端读取器清理通过。无 Python 异常或缺字警告，仅 Qt 字体别名加载耗时提示。

8 项数值测试通过。约 0.4 秒静止确认延迟、匀速与静止不可仅由 IMU 区分、没有绝对位置参考等限制仍然存在。详见 `gui/ESKF.md` 和 README 的 Bug 3。
