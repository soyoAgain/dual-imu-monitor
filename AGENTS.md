# AGENTS.md

本项目的人机协作约定，供后续会话中的自动化助手遵循。

## 工作偏好

- **数据采集时长**：单次采集 20 秒以内即可，不主动运行长时间测试（如 10 分钟稳定性测试）；仅当用户明确要求或属于最终验收矩阵时才长时间运行。
- **文档更新**：每次完成实验或阶段后，更新对应文档（`README.md`、`NETWORK_DIAGNOSIS.md`、`ICM20608_FUSION_PIPELINE.md` 等）。
- **提交粒度**：每个阶段单独提交，工具/数据与文档可分两次提交；提交前保持工作区干净。
- **不提交**：构建产物、`third_party/` 工具链、临时文件与缓存（见 `.gitignore`）。
- **开发板操作**：先确认 SSH/网络状态；SSH 偶发超时（eth0 曾出现 Link Down/Up）可重试一次。
